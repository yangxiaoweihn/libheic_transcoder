[package]
name = "heic_transcoder"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
heif-rs = "26.7"
image = { version = "0.25", default-features = false, features = ["jpeg"] }

[profile.release]
lto = true
opt-level = 3
strip = "symbols"